function vaidateForm() {
    var isValid = true;//,
   /*     name = $("#PlayerName"),
        phone = $("#Phone"),
        email = $("#Email");
    $("form div").removeClass("has-error");
    $("form span.glyphicon").hide();
    if (!$.trim(name.val())) {
        isValid = false;
        name.parent().addClass("has-error");
        name.parent().find("span.glyphicon").show();

    }
    if (!/^([\w-]+(?:\.[\w-]+)*)@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$/i.test(email.val())){
        isValid = false;
        email.parent().addClass("has-error");
        email.parent().find("span.glyphicon").show();
    }
    if (!/^((8|\+7)[\- ]?)?(\(?\d{3}\)?[\- ]?)?[\d\- ]{7,10}$/.test(phone.val())) {
        isValid = false;
        phone.parent().addClass("has-error");
        phone.parent().find("span.glyphicon").show();
    }*/

    return isValid;
}

